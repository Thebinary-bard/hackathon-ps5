# Backend API base URL (used for regular fetch calls — goes through Vite proxy)
VITE_API_BASE_URL=

# Direct backend URL used ONLY for OAuth redirects (must NOT go through Vite proxy)
VITE_BACKEND_URL=http://localhost:5000
